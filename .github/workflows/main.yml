name: Deploy and Test

on:
  push:
    branches:
      - main  # Cambia la rama si deseas otra

jobs:
  deploy_and_test:
      runs-on: self-hosted    
      steps:
      - name: Checkout
        uses: actions/checkout@v1

      - name: Ejecutar script de despliegue
        run: |
          echo "Hello world!"
          #echo "Ejecutando script de despliegue..."

      - name: Apaagar el runner después del workflow
        run: |
          echo "Cerrando el runner ..."
          sudo systemctl stop.actions.runner.${{ github.repository_owner }}.${{ github.event.repository.name }}.*
        if: always
