name: Deploy to ECU

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run deployment script locally (since this is the ECU)
        run: |
          echo "🚀 Ejecutando el script de despliegue local en la ECU..."
          bash ~/CI-CD/deploy_ecu.sh
